#include <stdio.h>

int main()
{
    int a, b;
    int temp;
    scanf_s("%d %d", &a, &b);
    while (b != 0)
    {
        temp = b;
        b = a % b;
        a = temp;
    }
    printf("%d", a);
  
}
