#include <stdio.h>
#include <math.h>

int main()
{
    int a, b, c, p;
    printf("请输入第一边\n:");
    scanf_s("%d", &a);
    printf("请输入第二边\n:");
    scanf_s("%d", &b);
    printf("请输入第三边\n:");
    scanf_s("%d", &c);
    p = (a + b + c) / 2;
    printf("面积为%f\n", sqrt(p * (p - a) * (p - b) * (p - c)));
}
//假设在平面内，有一个三角形，边长分别为a、b、c，三角形的面积S可由以下公式求得:
//S=√[p(p-a)(p-b)(p-c)]
//而公式里的p为半周长:
//p=(a+b+c)/2
